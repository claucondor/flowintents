# Environment — NEVER commit private keys
.env
.env.*
!.env.example

# Flow CLI
.flow/
# Emulator private key — contains dev key, do not commit to mainnet repos
emulator-account.pkey
# Downloaded contract sources (regenerated by `flow dependencies install`)
imports/

# Node
node_modules/

# Build artifacts
dist/
out/

# Foundry
evm/out/
evm/cache/
evm/broadcast/*/
evm/deployments/*.json

# OS
.DS_Store
